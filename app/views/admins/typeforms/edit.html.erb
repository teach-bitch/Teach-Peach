<%= render 'errors', typeform: @typeform %>

<h1>Editing Typeform</h1>

<%= render 'form', typeform: @typeform %>

<%= link_to 'Show', admins_typeform_path(@typeform.id) %>
<%= link_to 'Back', admins_typeforms_path %>
