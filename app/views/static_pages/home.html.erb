






<!--Carousel Wrapper-->
<div id="carousel-example-2" class="carousel slide carousel-fade" data-ride="carousel">
  <!--Indicators-->
  <ol class="carousel-indicators">
    <li data-target="#carousel-example-2" data-slide-to="0" class="active"></li>
    <li data-target="#carousel-example-2" data-slide-to="1"></li>
  </ol>
  <!--/.Indicators-->
  <!--Slides-->
  <div class="carousel-inner" role="listbox">
    <div class="carousel-item active">
      <div class="view">
        <%= image_tag "peach4_crop", alt: "placeholder image 0", class: "d-block w-100" %>
        <div class="mask rgba-black-light"></div>
      </div>
      <div class="row">
        <div class="carousel-caption">
          <h2 class="h2-responsive">Découvre la sexualité avec Teach Peach</h2>
        </div>
      </div>
    </div>
    <div class="carousel-item">
      <!--Mask color-->
      <div class="view">
        <%= image_tag "peach3_crop", alt: "placeholder image 0", class: "d-block w-100" %>
        <div class="mask rgba-black-strong"></div>
      </div>
      <div class="carousel-caption">
        <h2 class="h2-responsive"> Influence le contenu en partageant anonymement ton experience ! </h2>

        <% if Typeform.last %>
        <h3 class="h3-responsive btn-carousel">
          <%= link_to "Partage ton avis sur " + @typeforms.last.title.downcase, typeform_path(Typeform.last.id), class: 'btn btn-primary btn-block btn-carousel' %>
        </h3>

        <% end %>

      </div>
    </div>

  </div>
  <!--/.Slides-->
  <!--Controls-->
  <a class="carousel-control-prev" href="#carousel-example-2" role="button" data-slide="prev">
    <span class="carousel-control-prev-icon" aria-hidden="true"></span>
    <span class="sr-only">Previous</span>
  </a>
  <a class="carousel-control-next" href="#carousel-example-2" role="button" data-slide="next">
    <span class="carousel-control-next-icon" aria-hidden="true"></span>
    <span class="sr-only">Next</span>
  </a>
  <!--/.Controls-->
</div>
<!--/.Carousel Wrapper-->



<% @articles.each do |article|%>
<div class="col-md-6 px-0">
  <div class="card card-background card-background-product card-no-shadow" style="background-image: url('')">
    <div class="card-body">
      <h3 class="card-title">
        <%= article.title %>
      </h3>
      <p class="card-description">
      
        <%= raw article.content.split[0..10].join(" ") %>...
      </p>
      <%= button_to "Voir l'article", article_path(article), class: 'btn btn-neutral', method: :get %>
    </div>
  </div>
</div>
<% end%>
<div>
  <% if user_signed_in? %>
  <% if @user.subscription_id == nil %>
  <%= link_to "Je m'abonne", new_subscription_path, class: 'btn btn-primary' %>
  <% else %>
  <% end %>
  <% end %>
  <div>
    <% if @articles.last %>
    <td>
      <%= @articles.last.title %>
    </td>

    <td>
      <%= link_to "Voir notre dernier article", @articles.last %>
    </td>
    <% end %>
    _______________________________________
    <table>
      <thead>
        <tr>
          <th>Titre</th>
          <th>Contenu</th>
          <th colspan="3"></th>
        </tr>
      </thead>

      <tbody>
        <% @articles.each do |article| %>
        <tr>
          <td>
            <%= article.title %>
          </td>
          <td>
            <%= article.content %>
          </td>
          <td>
            <%= link_to "Voir l'article", article %>
          </td>
        </tr>
        <% end %>
      </tbody>
    </table>